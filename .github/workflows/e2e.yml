name: E2E (Espresso + Gradle Managed Device)

on:
  push:
    tags:
      - 'e2e-*'
  workflow_dispatch:

jobs:
  rename-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5

      - name: Enable KVM group perms
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v4

      # Cache the whole AVD + emulator state: GMD re-downloads the system image
      # and recreates the AVD every run otherwise (~2-4 min per run). No
      # untrusted input flows into this — static paths only.
      - name: Cache AVD + system images
        uses: actions/cache@v4
        with:
          path: |
            ~/.android/avd
            ~/.android/cache
            ~/.gradle/caches/jars-*
          key: ${{ runner.os }}-gmd-avd-v1

      # Persist the Gradle build cache between runs: org.gradle.caching=true is
      # set, but without persisting build-cache-1 every run recompiles all
      # unchanged Kotlin + re-assembles APKs (~3-4 min). Keyed by the workflow
      # file so CI-only changes don't poison the cache.
      - name: Cache Gradle build outputs
        uses: actions/cache@v4
        with:
          path: ~/.gradle/caches/build-cache-1
          key: ${{ runner.os }}-gradle-build-v1-${{ hashFiles('.github/workflows/e2e.yml', '**/*.gradle*', 'gradle/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-build-v1-

      # GMD: AGP provisions, boots, installs, runs, tears down the emulator.
      # Gradle's exit code fails when tests fail (no am-instrument trap).
      # swiftshader_indirect: GitHub runners have no hardware GPU.
      - name: Run E2E on Gradle Managed Device (Pixel 5, API 34)
        run: >
          ./gradlew e2eApi34IrisDebugAndroidTest --no-daemon
          -Dorg.gradle.workers.max=2
          -Pandroid.testoptions.manageddevices.emulator.gpu=swiftshader_indirect
          -Pandroid.testInstrumentationRunnerArguments.class=com.m57.hermescontrol.e2e.RenameE2eTest,com.m57.hermescontrol.e2e.RenameFailureE2eTest,com.m57.hermescontrol.e2e.RailReorderE2eTest

      - name: Upload instrumented results on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-results
          path: app/build/outputs/androidTest-results/**/*.xml
          retention-days: 14
