# Project-wide Gradle settings.
#
# ⚠️ DEPENDENCY VERIFICATION — REMOVED 2026-09-10 (Likivik)
#   gradle/verification-metadata.xml was deleted because every NEW dependency
#   (CrashWatcher, emoji-kt, the compose-screenshot plugin) hit the checksum
#   wall and needed manual hashing, blocking iteration on a hobby/fork repo.
#   TODO(security): RE-ENABLE before this repo is used for anything
#   user-facing. Restore by regenerating the metadata and this is still valid:
#       ./gradlew --write-verification-metadata sha256 help
# then commit the regenerated gradle/verification-metadata.xml.
#
# Enable the screenshotTest source set (compose screenshot plugin requirement).
android.experimental.enableScreenshotTest=true
#
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# Enables Gradle Build Cache.
# See https://docs.gradle.org/current/userguide/build_cache.html
org.gradle.caching=true
# Likivik: Gradle Configuration Cache DISABLED — AGP 9.1.1 `mergeIrisReleaseNativeLibs`
# fails to serialize into the config cache (FilteredFileCollectionSpec /
# ResolutionBackedFileCollection), failing CI with "Configuration cache state
# could not be cached". Build cache alone keeps rebuilds fast.
# Enables Gradle Configuration Cache, the preferred Gradle execution mode.
# See https://docs.gradle.org/current/userguide/configuration_cache.html
org.gradle.configuration-cache=false
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app"s APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
